# Subscription-Tracker
A RESTful API using express.js and MongoDB that manages user Subscriptions. It sends subscriptions expiry reminders on Intervals based on pre-set frequencies. Has build in Mail, SMS and Push notifications.
